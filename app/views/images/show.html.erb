<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @image.title %>
</p>

<p>
  <img src=<%= @image.url %> style='width: 500px' alt=<%= @image.title %>>
  <p>
    <strong>Ratings: <strong>
  </p>
    <% @ratings.each do |rating| %>
      <p>
        <%= rating.value %>
      </p>
      <% end %>

</p>
<%= link_to('Add Rating', new_rating_path(image_id: @image))  %>
<%= link_to 'Edit', edit_image_path(@image) %> |
<%= link_to 'Back', images_path %>
